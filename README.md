## Setup

This project uses TensorFlow, which currently has Windows wheels for Python 3.10 through 3.13, not 3.14.

Use `uv` with Python 3.13 in this project directory:

```bash
uv python install 3.13
uv sync
```
